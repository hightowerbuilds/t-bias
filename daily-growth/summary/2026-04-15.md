# Session Summary — 2026-04-15

## What We Did

### 1. Researched WICG HTML-in-Canvas Proposal
Fetched and analyzed the full WICG HTML-in-Canvas spec and Chromium implementation
(behind `chrome://flags/#canvas-draw-element`). Evaluated the four proposed primitives:
`layoutSubtree`, `drawElementImage`, `onpaint`/`requestPaint`, and `ElementImage`.

Produced a focused analysis of how each primitive maps to specific pain-points in our
canvas terminal — accessibility blind spot, OSC 8 hit-testing complexity, scroll
duplicate-line bug, OSC 133 prompt navigation.

### 2. Wrote the Integration Design Document
Created `docs/html-in-canvas-integration.md` — a full implementation guide treating
the API as production-ready. Covers five features, TypeScript declarations, rollout
sequence, resize/DPR handling, and known open spec questions.

### 3. Initialized Git and Created Feature Branch
- `git init` + initial commit to establish baseline on `main`
- Created branch `feat/html-in-canvas` for the integration work

### 4. Implemented HTML-in-Canvas Integration (Steps 1–5)

All five integration steps landed in a single commit on `feat/html-in-canvas`.

**New files:**
- `src/html-in-canvas.d.ts` — TypeScript augmentations for `HTMLCanvasElement`,
  `CanvasRenderingContext2D`, `PaintEvent`, `ElementImage`
- `src/terminal/htmlInCanvas.ts` — `isHtmlInCanvasSupported()` feature probe

**Changes to `src/terminal/TerminalHost.ts`:**

| Feature | What it does |
|---|---|
| `isHtmlInCanvasSupported()` gate | Zero-regression fallback; existing RAF path used on all other browsers |
| `textCanvas.layoutSubtree = true` | Canvas children enter the browser accessibility tree and hit-testing |
| `textCanvas.setAttribute("role", "application")` | Screen readers announce "Terminal" instead of a blank image |
| A11y row layer (`buildA11yRows` / `syncA11yLayer`) | One invisible `<div>` per viewport row; text synced from `getActiveRowText()` for dirty rows only; VoiceOver/NVDA can now read terminal output |
| OSC 8 link pool (`acquireLink` / `syncLinkLayer`) | Reusable `<a>` pool with correct `href` + `aria-label`; screen readers see "link: https://…" at URL spans; visual hover/click unchanged |
| OSC 133 prompt sections (`buildPromptSections` / `syncPromptAnchors`) | `<section aria-label="Shell prompt">` shown at MARK_A rows; screen readers locate prompt boundaries |
| `onpaint` render loop (`scheduleTextDrawHIC` / `onPaintFired`) | RAF phase flushes PTY writes + syncs DOM, then calls `requestPaint()`; pixels drawn inside `onPaintFired` after browser layout settles — closes the dirty-state race window that causes duplicate lines on scroll |
| Debug overlay | Shows `hic: on/off` to confirm which path is active |

**Verified:** `tsc --noEmit` zero errors · `vitest run` 58/58 pass · `vite build` clean

### 5. Renamed App to "t-Bias Tauri"
- `tauri.conf.json`: `productName` and window `title` → `"t-Bias Tauri"`
- `Cargo.toml`: package `description` updated

### 6. Built Release App + Deployed to Desktop
- `cargo tauri build` → compiled in 35s
- Output: `t-Bias Tauri.app` + `t-Bias Tauri_0.1.0_x64.dmg`
- `.app` copied to `~/Desktop/t-Bias Tauri.app`

### 7. Updated App Icons
- Cropped `tbias-tauri.jpg` to a square (`tbias-tauri-square.jpg`).
- Used `tauri icon` CLI to regenerate all icon assets (`.icns`, `.ico`, `.png`, and mobile mipmaps) in `src-tauri/icons`.
- Verified 53 new/modified files in the icon set.

### 8. Pushed Everything to GitHub
- Committed all icon changes and project updates.
- Pushed `main` to the remote repository: `https://github.com/hightowerbuilds/t-bias`.

### 9. Final DMG Build
- Performed a clean `bunx @tauri-apps/cli build`.
- Generated the final production DMG: `t-Bias Tauri_0.1.0_x64.dmg`.

### 10. Removed HTML-in-Canvas (Reverted to Pure Canvas RAF)
Ripped out the entire HTML-in-Canvas integration from `TerminalHost.ts`:
- Deleted `src/html-in-canvas.d.ts` and `src/terminal/htmlInCanvas.ts`
- Removed 8 class properties, all a11y/link/prompt layer methods, and the dual-path render logic
- Collapsed `scheduleTextDraw` back to a single direct RAF path
- Zero regressions: `tsc --noEmit` clean, 58/58 tests pass

### 11. Added File Explorer + Code Editor Tab Types

Extended the pane/tab system with two new leaf types:

**Rust backend (`src-tauri/src/fs_ops.rs`):**
7 new IPC commands using `std::fs` — `read_dir`, `read_file`, `write_file`, `move_entry`, `create_dir`, `delete_entry`, `get_home_dir`. Hidden files skipped; results sorted dirs-first alphabetically.

**Pane tree (`src/pane-tree.ts`):**
Added `FileExplorerPane` and `EditorPane` union members, `leafIds()` helper, fixed guards for all non-split leaf types.

**File Explorer (`src/FileExplorer.tsx`):**
DOM-based SolidJS component — lazy expand/collapse, HTML5 native drag-and-drop (file moves via `invoke(MOVE_ENTRY_CMD)`), double-click to open in editor, context menu (New File/Folder, Rename, Delete), inline rename inputs. Uses `createStore` + `produce` to avoid TypeScript spread-args errors on nested tree mutations.

**Code Editor (`src/editor/`):**
- `EditorBuffer.ts` — pure text buffer with cursor, selection, undo/redo command pattern
- `Tokenizer.ts` — regex-based syntax highlighter for TS/JS and Rust (no npm dependency)
- `EditorHost.ts` — canvas renderer reusing `GlyphAtlas` + Pretext.js cell metrics; line number gutter, tokenized rendering, keyboard (Cmd+S save, Cmd+Z undo, copy/paste), mouse click+drag selection, wheel scroll
- `Editor.tsx` — SolidJS wrapper with ResizeObserver and focus management

**App integration:**
- Tab bar: folder icon button (File Explorer) and `</>` button (Code Editor)
- `Cmd+Shift+E` shortcut for File Explorer
- `openFileInEditor(filePath)` handler wires explorer double-click → new editor tab
- Session save/restore extended to include all three leaf types

### 12. Native macOS Menu Bar — View Menu
Since the tab bar buttons weren't visible (blank screen on first run), added a native macOS menu bar entry as a reliable fallback:

**`src-tauri/src/lib.rs`:** In `setup`, uses `tauri::menu::{MenuBuilder, SubmenuBuilder, MenuItem}` to create `View > File Explorer` and `View > Code Editor`. Menu click events are forwarded to the frontend via `app_handle.emit()`.

**`src/App.tsx`:** In `onMount`, listens for `"open-file-explorer"` and `"open-code-editor"` Tauri events via `window.__TAURI__.event.listen`. Each listener opens the corresponding tab. Unlisten functions called in `onCleanup`.

---

## Key Decisions

**Why `onpaint` instead of pure RAF for the scroll bug:**
The duplicate-line bug is a race between `scrollUp` (which rotates the row-index map
and marks rows dirty) and the RAF render loop reading that dirty state. With `onpaint`,
the browser guarantees layout is settled before we read `getRenderState()` — no
interleaving possible within a single rendering cycle.

**Why keep `detectHoveredUrl` even with `<a>` elements:**
`cursorCanvas` sits on top and intercepts all mouse events. `<a>` elements inside
`textCanvas` don't receive native mouse events from above. The `<a>` pool is
accessibility-only; the visual pointer cursor and Cmd+Click still come from our
existing hover-detection code.

**Why not remove OSC 8 underline drawing from `drawSelectionLayer`:**
Same reason — the `<a>` elements are transparent and positioned behind `cursorCanvas`.
The underlines must remain in our canvas drawing code to be visible.

---

## What to Watch
- The `onpaint` timing relative to RAF varies by browser build; monitor for added
  latency vs the baseline at `chrome://flags/#canvas-draw-element`
- `scrollIntoView` on `layoutSubtree` children is still an open spec question
  (WICG issue #12) — don't wire prompt jump to it yet
- Test with VoiceOver: toggle `chrome://flags/#canvas-draw-element`, open terminal,
  navigate with VO+Right to confirm row text is announced
