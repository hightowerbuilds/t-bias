# Session Summary — 2026-04-19

## What We Did

### 1. Terminal Trust Hardening — Process Lifecycle (Phase 1)
Addressed the most critical gap in the terminal's reliability: PTY processes surviving app quit.

- Added `PtyState::close_all()` in Rust to terminate every tracked PTY child process
- Restructured `lib.rs` to hook into Tauri's `RunEvent::Exit`, guaranteeing cleanup regardless of how the app exits (Cmd+Q, window close, forced quit)
- Replaced the fire-and-forget `beforeunload` handler with Tauri's `onCloseRequested` so the shell registry shutdown is properly awaited before the window closes
- Defined and documented the lifecycle contract:
  - Pane close: SIGHUP → SIGCONT → SIGTERM → SIGKILL the process group
  - Tab close: close all panes in tab
  - App quit: shell registry updated, then all PTYs terminated by Rust exit handler
  - Background jobs: terminated with the shell (not detached)
  - Shell exits on its own: "[Process exited]" shown, pane stays open

### 2. Close Confirmation Dialog
Added an in-app confirmation modal when closing tabs, panes, or the app while foreground processes are running. Shows the process names (e.g. "vim is still running. Closing will terminate it.") with Cancel/Close options. Styled to match the dark theme.

### 3. Modularized App.tsx
Broke the 1,190-line App.tsx into focused modules:

- `ShellLanding.tsx` (209 lines) — Shell registry modal UI
- `TabBar.tsx` (155 lines) — Tab bar rendering
- `CloseConfirmDialog.tsx` (85 lines → later 47 lines after CSS) — Close confirmation modal
- `useShellRegistry.ts` (126 lines) — Shell record state + IPC operations

App.tsx dropped from 1,190 to 690 lines.

### 4. CSS Design System
Extracted all repeated inline styles into a proper CSS architecture:

- `src/styles/theme.css` — CSS custom properties for colors, fonts, spacing, radii, shadows, z-index layers, plus the CSS reset (moved from index.html)
- `src/styles/components.css` — Reusable classes: `.btn` variants, `.modal-backdrop`, `.modal-card`, `.section-label`, `.input-field`, `.pane-active-border`
- Migrated all 10 component files to use `var(--...)` references and CSS classes
- Moved `blog-preview.css` into `src/styles/` for consistency
- Cleaned up `index.html` to pure HTML scaffolding

### 5. Lifecycle QA Testing (Phase 1.3)
Ran the 12-test lifecycle QA checklist against a live build:

- **9 passed** (Tests 1–6, 9, 10): foreground shell, long-running child, vim, less, tmux, Claude CLI, shell self-exit, background jobs all terminate correctly on pane close
- **Test 7 failed** (tab close with multiple panes): Cmd+D split caused TerminalView to unmount/remount, losing screen state — Claude appeared to close
- **Test 8 skipped**: app quit behavior handled by RunEvent::Exit, no additional verification needed
- **Test 11 failed** (rapid `yes` + close): crashed the app — race between PTY output and pane disposal
- **Test 12**: not yet tested (close during shell startup)

### 6. TerminalHost Cache — Fix for Test 7
Fixed the pane-split screen-loss bug by caching TerminalHost instances across component remounts:

- **`Renderer.ts`**: Added `reattach(canvas)` to switch canvases while preserving the glyph atlas
- **`TerminalHost.ts`**: Added `detach()` (save state, teardown DOM/timers) and `reattach(canvas)` (rebuild DOM, re-register listeners, full redraw)
- **`Terminal.tsx`**: Added `hostCache` map and `destroyTerminalHost()` export — on mount checks cache and reattaches existing host instead of creating a blank one; on cleanup detaches instead of disposing
- **`App.tsx`**: Calls `destroyTerminalHost()` when actually closing panes (doCloseActivePane, doRemoveTab)

Root cause: Solid's `Switch/Match` in `PaneNode` tears down the old component and mounts a new one when the pane tree restructures (e.g. splits change the root from a terminal to a split node). Without the cache, the new TerminalView created a fresh TerminalHost with blank screen even though the PTY was still alive.

## Roadmap Progress

### Terminal Trust Hardening
- Phase 1.1 (Define lifecycle rules): 5/5 complete
- Phase 1.2 (Backend ownership): 5/5 complete (race condition handling resolved)
- Phase 1.3 (Verify lifecycle behavior): 9/12 tested, 2 fixes applied, 3 need re-test
- Overall roadmap: ~21/52 items complete (~40%, up from ~37%)

## What's Next
- Re-test Tests 7, 11, and 12 in a live build to close out Phase 1.3
- Phase 2: Session fidelity improvements (editor file identity, active tab persistence, session semantics)
- Phase 3: Persistence architecture (compact JSON, atomic writes, stress testing)
