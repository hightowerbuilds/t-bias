# Session Summary — 2026-04-21

## What We Did

### 1. Shell Registry — Full-Window Modal
Converted the shell registry from a centered card modal to a full-window modal overlay, matching the prompt stacker's pattern:

- Added fixed overlay wrapper in `App.tsx` with `var(--bg-overlay)` and `var(--z-shell-landing)`
- Removed `modal-backdrop` / `modal-card` wrapper from `ShellLanding.tsx`, replaced with flex layout that fills the overlay
- Removed "New Shell" button (redundant with tab bar)
- Changed conditional "Done" button to an always-visible "Close" button

### 2. Global Monospace Font
Set `font-family: var(--font-mono)` on `html, body` in `theme.css` so all UI elements (including the shell registry modal) inherit the monospace font stack instead of the browser default.

### 3. App Icon Update
Regenerated all Tauri icon sizes from the updated `tbias-tauri.jpg` (blue Tobias). Clean rebuild to embed new `icon.icns` into the binary. Cleared macOS icon cache.

### 4. FlipExplorer Single-Click Fix
Fixed a bug where folders required two clicks to expand. Root cause: `toggleExpand` lazy-loads children by mutating a plain object, but SolidJS doesn't react to direct mutations. Added a `treeVersion` signal that bumps after loading, forcing the `Show` condition to re-evaluate.

### 5. Prompt Queue Footer — Taller + Truncated
- Doubled footer height from 46px to 88px when expanded
- Changed pill layout from horizontal scroll to `flex-wrap` so prompts wrap to next row
- Truncated prompt text to first 5 words followed by `...` instead of 56-character cutoff

### 6. Codebase Quality Audit & Cleanup
Ran a full audit across frontend, backend, and IPC boundary. Fixed all identified issues:

**Frontend:**
- Replaced 5 hardcoded `Menlo, Monaco, 'Courier New', monospace` strings with `var(--font-mono)` in FileExplorer and FlipExplorer
- Replaced 3 raw `invoke("...")` calls with proper `_CMD` constants in App.tsx, FileExplorer.tsx, PromptQueueFooter.tsx
- Removed 2 `[t-bias DEBUG]` console.log statements from Terminal.tsx and Screen.ts
- Removed 7 unused TypeScript types from `ipc/types.ts` (SpawnShellArgs, WriteToPtyArgs, ResizePtyArgs, ClosePaneArgs, GetPaneForegroundProcessNameArgs, PtyOutputEvent, PtyExitEvent)

**Rust Backend:**
- Extracted `tbias_dir()` from 3 duplicate definitions (prompt_stacker, session, shell_registry) into shared `persistence.rs`
- Fixed dead `exact` variable in `fs_ops.rs::resolve_existing_dir_path` — was always `false`, now correctly returns inline values

### 7. Terminal Rendering — Synchronized Output (DEC 2026)
Implemented the synchronized output protocol that TUI apps (Claude Code, Gemini, Ink-based tools) rely on to prevent partial frame tearing. When mode 2026 is enabled, the renderer defers canvas draws until the app signals the update is complete. Includes a 200ms safety timeout to prevent frozen display if an app crashes mid-sync.

### 8. Terminal Rendering — Viewport-Aware Scroll
Fixed scrollback rendering — the renderer was always reading from the active screen's typed arrays regardless of viewport position. Added `RowSource` abstraction so each visible row resolves to the correct data source (scrollback ring buffer or active screen). Also fixed tab visibility leak (`visibility: "visible"` → `"inherit"` in Panes.tsx).

### 9. Rust VT Backend (Major Architecture Change)
Migrated VT escape sequence processing from JavaScript to Rust using the `vte` crate (the same parser powering Alacritty). This is the biggest architectural change in the project's history.

**New Rust module (`screen.rs`, ~900 lines):**
- `ScreenBuffer` — complete terminal state machine: cell grid, cursor, scroll regions, SGR (16+256+truecolor), alt screen, scrollback, insert/delete, erase, synchronized output
- Implements `vte::Perform` trait wired to the battle-tested `vte` parser
- `ScreenState` wraps buffer + parser; `process(bytes)` feeds raw PTY output
- `take_frame()` returns dirty-checked `ScreenFrame` for frontend rendering

**PTY integration (`pty.rs`):**
- Per-pane `SharedScreen` created on spawn, cleaned up on close
- Reader thread feeds raw bytes through `ScreenState::process()` on every read
- Emits `frame-ready-{id}` signal (suppressed during sync output)
- `resize_pty` also resizes the Rust screen buffer
- New `get_frame` IPC command returns latest `ScreenFrame`

**Frontend frame renderer:**
- `CanvasRenderer.drawFrame()` renders a `ScreenFrame` using the existing glyph atlas
- `TerminalHost.drawFrame()` delegates to renderer
- Terminal.tsx listens for `frame-ready` events, fetches frame on RAF via `get_frame`
- JS pipeline stays active for input, selection, search, scrollback
- Both pipelines run in parallel during transition

### 10. UI Polish
- Removed subtitle text from Prompt Stacker header
- Removed all border-radius from Prompt Stacker containers
- Updated README to accurately describe the vte-based architecture

## Test Coverage

- **115 frontend tests** passing (terminal core, workspace state, session state, prompt stacker store)
- Rust compiles clean with `cargo check`

## Commits

1. `02d7280` — Shell registry full-window modal, global monospace font, UI cleanup
2. `21a356e` — Synchronized output, viewport-aware scroll, code quality audit
3. `979de5b` — Rust VT backend with vte crate, frame-based rendering pipeline
4. Prompt stacker UI polish, border radius removal
