# Session Summary — 2026-07-03

Pivot day: decided to rebuild t-bias from scratch on **Deno Desktop**, scorched the old
Tauri/Rust app, and stood up a working Phase 0 spine.

## What We Did

### 1. Codebase Review
Walked the existing app end to end (Tauri 2 + Rust PTY backend, SolidJS frontend, xterm.js
terminal). Confirmed the memory note was stale — the terminal had already migrated off the
custom canvas VT parser to xterm.js. Identified `pty.rs` (process lifecycle, signal handling,
`proc_pidinfo` cwd/foreground tracking) as the crown jewel worth carrying forward.

### 2. Rebuild Decision + Research
Verified (via web research, since it's recent) that **Deno Desktop** is real — shipped in
**Deno 2.9** (June 2026): `deno desktop` CLI, native WebView backend (~68MB vs ~309MB CEF),
`Deno.BrowserWindow`/`window.bind()` in-process bindings, auto-detects SolidStart/TanStack
Start/Vite, `Deno.autoUpdate()`.

**Locked the stack:**
- Deno Desktop shell (native WebView)
- Vite + SolidJS **SPA** (not SSR — no benefit for a local app)
- TanStack Router + TanStack Query
- xterm.js + WebGL (carried over)
- **Rust PTY sidecar** reusing `pty.rs` (not pure-Deno FFI)
- `node:sqlite` + Drizzle ORM
- tinykeys for hotkeys
- Optional AI (Anthropic SDK, server-side in Deno) — Phase 6

**Key call — PTY strategy.** Deno has no native PTY (`Deno.Command` gives pipes, not a
controlling TTY). Corrected an early lean toward raw-libc FFI: FFI is a *mechanism*, not a
language choice, and terminal perf is bottlenecked by xterm.js/WebGL rendering, not PTY
transport. So the PTY layer optimizes for correctness → **reuse `pty.rs` as a Rust sidecar**.

### 3. Roadmap
Wrote `daily-growth/roadmaps/deno-rebuild-roadmap.md` — a living plan: goal, locked stack,
runtime architecture diagram, open questions, phased build order (0–6), risks, decisions log.
Build order deliberately front-loads the risky PTY streaming path in Phase 1.

### 4. Scorched Earth
`git rm` of the entire old app — `src/`, `src-tauri/`, build config, and a `node_modules/`
that had been committed (3,298 deletions). Preserved reuse material to scratchpad seeds:
- `rust-pty-seed/` — `pty.rs`, `shell_registry.rs`, `session.rs`, `fs_ops.rs`, `config.rs`
- `frontend-seed/` — `pane-tree`, `workspace-state`, `session-state`, canvas, editor,
  prompt store, `XtermHost`
(Everything also recoverable from git history.)

### 5. Phase 0 — Scaffold (complete)
Upgraded Deno 2.7.11 → **2.9.1** (`brew upgrade deno`; the brew build has `deno upgrade`
disabled). Empirically probed the desktop runtime — confirmed `deno desktop <entry>` *builds*
an `.app` via the laufey webview backend (v0.5.0), globals injected only under that runtime.

**Built the app spine:**
- SolidJS SPA (Vite 8, solid 1.9): TanStack Router (1.170) with Home/Terminal routes,
  TanStack Query (5.101), tinykeys (4.0)
- `main.ts` — Deno entrypoint that serves the built `dist/` plus the backend bridge:
  `GET /api/ping` (HTTP) and `WS /ws` (echo now; **PTY output stream reuses it in Phase 1**).
  Guarded `BrowserWindow` block so the same file runs as a browser web app *and* a native shell.

**Dev workflow established:** develop as a normal web app (`deno task serve` → browser,
fast), `bun run build` rebuilds the SPA, `deno task desktop` bundles the native `.app`.

### 6. Phase 1/2 Validation
Closed out the terminal-spine validation gap by writing a PTY harness that drives a real
`/pty` WebSocket session through `main.ts` → `pty_bridge.ts` → Rust sidecar.

**Tested commands:**
- `echo` round-trip ✅
- `vim -u NONE -c 'q!'` (launch + exit) ✅
- `claude --version` ✅
- `yes | head -n 5000` (heavy output backpressure) ✅
- `top -l 1 | head -n 20` (TUI batch smoke test; `htop` not installed) ✅

All five harness checks passed against the dev server. The spine handles login-shell startup,
interactive terminal teardown, and high-volume PTY streaming without dropped data. `htop` was
unavailable on this machine, so validation was delegated to `top` as an equivalent TUI batch
smoke test; `htop` can be added later if installed.

### 7. Phase 1/2 Packaging
Closed the sidecar-packaging gap.

- Updated `pty_bridge.ts` to resolve the sidecar from multiple candidates:
  - `TBIAS_PTY` env override
  - dev build at `cwd/sidecar/target/release/tbias-pty`
  - packaged `.app` next to the executable (`Contents/MacOS/tbias-pty`)
  - packaged `.app` in `Contents/Resources/sidecar/tbias-pty`
- Added `scripts/package-sidecar.ts`: copies the release binary into the bundle and re-signs
  with an ad-hoc identity.
- Added `deno task desktop:build` which runs `bun run build` → `deno task desktop` → package
  sidecar + re-sign.
- Verified the packaged `dist-app/t-bias.app` launches, serves the SPA, and runs a real login
  shell over `/pty` (tested `echo PACKAGED_APP_WORKS` inside the bundled app).

### 8. Commit
Staged all changes and committed the Phase 1/2 completion:

```
feat(phase-2): workspace tabs/splits/zoom + PTY sidecar packaging
```

### 9. Phase 3 — Persistence (first slice)
Started the persistence layer with Drizzle + `node:sqlite`.

- Added `drizzle-orm` and `@types/node` dependencies.
- Created `db/client.ts`: opens SQLite (`node:sqlite`), bridges it to Drizzle via the
  `sqlite-proxy` driver, and uses `Deno.execPath()` to pick an app-data dir for packaged
  builds or `data/` for dev.
- Created `db/schema.ts` and `db/migrate.ts`: `workspaces`, `tabs`, and `panes` tables with
  foreign keys and indexes.
- Created `db/workspace.ts`: `loadWorkspace()` and `saveWorkspace()` serialize/deserialize
  the pane tree.
- Added HTTP endpoints in `main.ts`: `GET /api/workspace` and `POST /api/workspace/save`.
- Updated `src/workspace/store.ts` with `hydrate()` / `snapshot()` and `src/workspace/Workspace.tsx`
  to load on mount and save on `beforeunload`.
- Added `data/` to `.gitignore`.

**Verification:**
- `tsc --noEmit` and `deno check` clean
- `/api/workspace` round-trip via curl: save → load returns the same tabs/panes ✅
- PTY harness still passes 5/5 after the changes ✅

**Caveat:** The packaged `.app` launched but did not expose an HTTP listener in this pass.
Likely the app-data path or `node:sqlite` behavior under the packaged runtime needs a
debugging round. Dev server is fully functional.

## Verification

- Frontend typecheck (`tsc --noEmit`): clean
- Backend typecheck (`deno check main.ts`): clean
- `GET /api/ping` → `{"ok":true,"runtime":"Deno 2.9.1",...}`
- SPA serves + deep-link `/terminal` falls back to index (router): `200`
- WebSocket `/ws`: `ws bridge open ✓` → `echo: ping-from-test`
- `deno desktop` builds `dist-app/t-bias.app` with the SPA embedded, launched on screen
- PTY harness: 5/5 real-app checks passed (echo, vim, claude --version, yes | head, top)
- Packaged `.app` serves `/api/ping` and runs a real shell over `/pty` ✅ (Phase 1/2)
- `/api/workspace` save/load round-trip works in dev ✅
- PTY harness still passes after persistence changes ✅

## Known Follow-ups

- Debug why the packaged `.app` no longer starts its HTTP listener after adding `node:sqlite`/DB path
- Pin the exact `Deno.BrowserWindow`/`window.bind()` API (docs thin; currently guessed + guarded)
- Confirm same-origin `ws://` works *inside* the packaged laufey webview
- Trim `node_modules/` from the desktop bundle (164MB → ~70MB; Vite already bundles the SPA)
- `htop` validation (if installed later) or decide if `top` batch mode is sufficient
- Decide if we need the `Contents/Resources/sidecar` fallback or if `Contents/MacOS` is enough

## Next

- Debug packaged app startup with `node:sqlite`/DB path.
- Then continue Phase 3: auto-save on layout changes, shell records, atomic shutdown save.
