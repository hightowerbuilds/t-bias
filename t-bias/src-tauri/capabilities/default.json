{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default capability for the terminal",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "shell:default",
    "core:event:default"
  ]
}
